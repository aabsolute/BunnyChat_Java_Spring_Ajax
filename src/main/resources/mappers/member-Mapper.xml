<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">
	<!-- Insert Member Information -->
	<insert id="insert">
		INSERT INTO MEMBER_INFO_TABLE
		( USER_ID
		, USER_EMAIL
		, USER_NAME
		, USER_PASSWORD
		, USER_AGE
		, USER_INFOMATION 
		, USER_regdate
		, USER_lastLogin)
		VALUES( 
		  #{userID}
		, #{userPassword}
		, #{userName}
		, #{userAge} 
		, #{userGender} 
		, #{userEmail} 
		, #{userInfomation} 
		, DEFAULT(`USER_regdate`) 
		, DEFAULT(`USER_lastLogin`) )
	</insert>
	<!-- 
	<select id="checkEmail" parameterType="String"  resultType="String"> resultType="member"
		SELECT user_EMAIL 
		FROM Member_Info
		where user_EMAIL = #{userEmail}
	</select> -->

</mapper>